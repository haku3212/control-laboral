# Telegram Setup

Telegram queda como opcion futura.

El flujo principal ahora es una sola APK con dos roles: operario y admin.

Si luego se agrega Telegram, los secretos se configuraran con `supabase secrets set` y nunca se guardaran en Flutter.
